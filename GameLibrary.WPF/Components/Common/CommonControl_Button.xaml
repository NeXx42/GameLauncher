<UserControl x:Class="GameLibary.Components.Common.CommonControl_Button"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameLibary.Components.Common"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">
    <Border x:Name="ctrl" CornerRadius="2" Background="#2C2C2C" BorderBrush="#FF444444" BorderThickness="1,1,1,1" ClipToBounds="False">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Direction="0" Opacity="0.1"/>
        </Border.Effect>
        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
    </Border>
</UserControl>
