<UserControl x:Class="GameLibary.Components.Control_GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameLibary.Components" xmlns:common="clr-namespace:GameLibary.Components.Common"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200">
    <Border Background="{DynamicResource BG_2}" CornerRadius="5,5,0,0">
        <Grid>
            <Grid Margin="25,25,25,25">
                <Label x:Name="lbl_Title" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="White" FontSize="24">Game Name</Label>

                <StackPanel Background="{DynamicResource BG_1}" Margin="150,50,150,50" VerticalAlignment="Top" Orientation="Vertical">
                    <Border x:Name="border" VerticalAlignment="Top" Height="490" >
                        <Image  x:Name="img_bg" Source="/Components/temp.jpeg" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                    </Border>

                    <StackPanel Margin="0,10,0,10" VerticalAlignment="Top" Background="#99000000" Orientation="Horizontal" HorizontalAlignment="Center">
                        <common:CommonControl_Button Margin="0,0,5,0" x:Name="btn_RegenMedia" Width="120" Label="Clear"/>
                        <common:CommonControl_Button Margin="0,0,5,0" x:Name="btn_Overlay" Width="120" Label="Open Overlay"/>
                        <ComboBox Margin="0,0,5,0" x:Name="inp_binary" Width="385"></ComboBox>
                        <common:CommonControl_Button Margin="0,0,5,0" x:Name="btn_Delete" Label="Delete" Width="120"/>
                    </StackPanel>
                </StackPanel>

                <Grid  VerticalAlignment="Bottom" Margin="0,0,0,50" Height="200">
                    <Label VerticalAlignment="Top" Content="Tags" HorizontalAlignment="Center" Foreground="White" FontSize="20"/>
                    <Border  Margin="15,40,15,50" Background="{DynamicResource BG_3}" VerticalAlignment="Top" CornerRadius="5">
                        <WrapPanel Margin="10" x:Name="cont_AllTags" HorizontalAlignment="Center">

                        </WrapPanel>
                    </Border>
                </Grid>

                <CheckBox x:Name="inp_Emulate" Content="Emulate" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="70,0,0,20" Foreground="White" RenderTransformOrigin="0.5,0.5">
                    <CheckBox.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="3" ScaleY="3"/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </CheckBox.RenderTransform>
                </CheckBox>

                <common:CommonControl_Button x:Name="btn_Browse" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="200" Height="50" Margin="0, 0, 210, 0" Label="Browse"/>
                <common:CommonControl_Button x:Name="btn_Launch"  VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="200" Height="50" Label="Launch"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
